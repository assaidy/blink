# NOTE: I know it shouldn't be public (in real projects).

PORT   = 8080
SECRET = d5ac9bf84c9ca850db768113549e6650b91010b00c5ad741def843dae0f55dc0 

POSTGRES_PORT      = 5432
POSTGRES_USER      = postgres
POSTGRES_PASSWORD  = mypassword
POSTGRES_NAME      = blink
POSTGRES_URL       = postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres_db:5432/${POSTGRES_NAME}?sslmode=disable
POSTGRES_URL_LOCAL = postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_NAME}?sslmode=disable
POSTGRES_DATA_PATH = ./docker_volumes/postgres/

VALKEY_PORT       = 6543
VALKEY_ADDR       = valkey_cache:6379
VALKEY_ADDR_LOCAL = localhost:${VALKEY_PORT}
VALKEY_DATA_PATH  = ./docker_volumes/valkey/

EMAIL_FROM               = noreply@blink.com
PAPERCUT_SMTP_HOST_LOCAL = localhost
PAPERCUT_SMTP_HOST       = papercut_smtp
